
1. Make sure MySQL is installed and ready.
2. Make sure flask is installed and install bcrypt (type in "pip install bcrypt" in the cmd and press enter)
3. in database.py, change the host,user and passwd to the correct values you set them to be.
4. run the database.py file
5. Remove the "setup()" call at the bottom of the page, and also remove "mycursor.execute("CREATE DATABASE items")" near the top.
6. Run the main.py file

